{% trans_default_domain 'FOSUserBundle' %}

<div class="ui page grid segment">
    <div class="fos_user_user_show column">
        <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
        <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
    </div>
</div>